# PHP Docker

## Primer setup de entorno

## Setup de PHP Docker
```bash
cd drupal_project
docker-compose up --build
```

## Setup de Drupal
```bash
cd drupal_project/application/web
composer install
```
